'use strict';

module.exports = function sometimesFails() {
    return require('helpers').delay().then(() => {
        if (Math.random() > 0.5) {
            throw Error('failure')
        }
        return 'success'
    })
}
