'use strict';

const inBox = require('z/wrapping/box')
const inPaper = require('z/wrapping/paper')
const inRibbons = require('z/wrapping/ribbons')
const inGlitter = require('z/wrapping/glitter')

//wrap a perfect gift
const theGift = 'Another boring tie'

async function giftWrapping(gift) {
    
    

    console.log(done)
    return done
}

giftWrapping(theGift)

// Check these out too
// const result = await giftWrapping(theGift)

// ; (async () => {
//     console.log(await giftWrapping(theGift))
// })()
